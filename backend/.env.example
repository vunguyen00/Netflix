# Copy this file to backend/.env and fill in the values before starting the server.
# Example:
#   cp backend/.env.example backend/.env
#
# MONGO_URI should point to your MongoDB instance. For local development you can
# use a connection string like the one below (requires a MongoDB server running
# on your machine).
MONGO_URI=mongodb://127.0.0.1:27017/dailywithminh

# Secret used to sign JWT tokens for the admin area.
JWT_SECRET=change-me

# Default admin credentials used by the seeding script/login endpoint.
ADMIN_USER=admin
ADMIN_PASS=super-secret-password
